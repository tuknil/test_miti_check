# Runtime run ledger (durable, but not source)
api/data/

# Local secrets / env (never commit tokens)
.env
*.env
!.env.example

# Editor swap files
*.swp

# IDE config
.idea/

# Compiled API binary (build artifact)
api/mitigation-check-api
